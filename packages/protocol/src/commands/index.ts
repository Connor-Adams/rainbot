export type {
  PlayParams,
  PlayCommandResult,
  SkipParams,
  SkipResult,
  QueueCommandResult,
  ClearResult,
  PauseResult,
  JoinParams,
  JoinResult,
  PingResult,
  VolParams,
  VolResult,
} from '../types/commands';

// Note: LeaveResult/StopResult live under voice types to avoid collisions.
