// Intentionally empty. `useAdminRunGuildId` (the Admin-local server
// selection) was removed in favor of the header `GuildPicker` /
// `useGuildStore`, which is now the single source of truth for the
// selected guild app-wide. File deletion is blocked in this environment,
// so this module is left in place with no exports.
