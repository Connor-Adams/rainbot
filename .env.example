# ============================================================================
# Multi-Bot Architecture Configuration
# ============================================================================

# Raincloud - Orchestrator Bot (receives Discord commands, serves web dashboard)
# This is the main bot that users interact with
RAINCLOUD_TOKEN=your_raincloud_bot_token_here
DISCORD_CLIENT_ID=your_raincloud_client_id_here

# Orchestrator Bot ID (REQUIRED) - Workers use this to auto-follow Raincloud into voice channels
# Get this from Discord Developer Portal → Your Raincloud App → Bot → Copy "Application ID"
# Or right-click the bot in Discord (with Developer Mode on) → "Copy User ID"
ORCHESTRATOR_BOT_ID=your_raincloud_bot_user_id_here

# Rainbot - Music Worker Bot (handles music playback)
# Create a separate Discord application for this bot
RAINBOT_TOKEN=your_rainbot_worker_token_here
RAINBOT_PORT=3001

# Pranjeet - TTS Worker Bot (handles text-to-speech)
# Create a separate Discord application for this bot
PRANJEET_TOKEN=your_pranjeet_tts_token_here
PRANJEET_PORT=3002

# HungerBot - Soundboard Worker Bot (handles sound effects)
# Create a separate Discord application for this bot
HUNGERBOT_TOKEN=your_hungerbot_soundboard_token_here
HUNGERBOT_PORT=3003

# Optional: Deploy commands to a specific guild for faster updates (recommended for development)
# If not set, commands will be deployed globally (takes up to 1 hour to propagate)
# DISCORD_GUILD_ID=your_guild_id_here

# Discord OAuth Configuration (for web dashboard)
DISCORD_CLIENT_SECRET=your_oauth_client_secret_here

# Optional: Restrict dashboard access to users with a specific role
# If not set, all authenticated Discord users can access the dashboard
# REQUIRED_ROLE_ID=your_required_role_id_here

# ============================================================================
# Worker URLs (for orchestrator to communicate with workers)
# In production, these should point to your deployed worker services
# ============================================================================
RAINBOT_URL=http://localhost:3001
PRANJEET_URL=http://localhost:3002
HUNGERBOT_URL=http://localhost:3003

# ============================================================================
# Infrastructure
# ============================================================================

# Redis Configuration (REQUIRED for multi-bot state management)
# Redis stores voice sessions, channel tracking, and worker status
REDIS_URL=redis://localhost:6379

# PostgreSQL Configuration (for statistics and user data)
POSTGRES_USER=rainbot
POSTGRES_PASSWORD=rainbot
POSTGRES_DB=rainbot
DATABASE_URL=postgresql://rainbot:rainbot@localhost:5432/rainbot

# Server Configuration (Raincloud API server)
PORT=3000
SESSION_SECRET=generate_a_random_secret_string_here
SESSION_STORE_PATH=./sessions
# When serving the UI as a separate service, set the public UI origin here
# DASHBOARD_ORIGIN=https://your-ui-domain

# OAuth Callback URL (optional - auto-detected on Railway)
CALLBACK_URL=http://localhost:3000/auth/discord/callback

# Feature Flags
DISABLE_AUTO_DEPLOY=false

# ============================================================================
# TTS Configuration (for Pranjeet worker)
# ============================================================================

# TTS Provider: openai, google
# OpenAI uses Whisper model and is much easier to set up (just API key)
TTS_PROVIDER=openai

# API key for the TTS provider
# For OpenAI: use your OpenAI API key
# For Google: use Google Cloud TTS API key
TTS_API_KEY=your_openai_or_google_api_key_here

# Alternative: Use OPENAI_API_KEY if you prefer (works for both STT and TTS)
# OPENAI_API_KEY=your_openai_api_key_here

# Voice Settings
# OpenAI voices: alloy, echo, fable, onyx, nova, shimmer
# Google voices: en-US-Neural2-J, en-US-Neural2-C, etc.
TTS_VOICE_NAME=alloy

# ============================================================================
# Voice Interaction Configuration (optional)
# ============================================================================

# Enable voice commands for music control
VOICE_INTERACTION_ENABLED=false

# Speech-to-Text Provider: google, openai, azure, aws, whisper
STT_PROVIDER=openai
STT_API_KEY=your_openai_api_key_here

# Voice Settings
VOICE_LANGUAGE=en-US

# ============================================================================
# Grok conversation (Pranjeet - /chat command in voice)
# ============================================================================
# Set GROK_API_KEY (or XAI_API_KEY) to enable. Set GROK_ENABLED=false to disable.
# GROK_API_KEY=your_xai_api_key_here
# Alternative: XAI_API_KEY=your_xai_api_key_here
# Optional: model name (default grok-4-1-fast-reasoning)
# GROK_MODEL=grok-4-1-fast-reasoning
# Debug: set LOG_LEVEL=debug (Pranjeet) for Grok request/response logging
# LOG_LEVEL=debug

# ============================================================================
# Storage Configuration (for soundboard sounds)
# ============================================================================

# S3-compatible storage for sound files
# Railway Bucket service variables:
STORAGE_BUCKET_NAME=your_bucket_name
STORAGE_ACCESS_KEY=your_access_key
STORAGE_SECRET_KEY=your_secret_key
STORAGE_ENDPOINT=https://your-endpoint.r2.cloudflarestorage.com
STORAGE_REGION=auto

# Alternative: Local storage directory (if S3 not configured)
# SOUNDS_DIR=./sounds

# ============================================================================
# UI Service Configuration (optional - separate UI service)
# ============================================================================
# Public Raincloud URL for browser calls
# VITE_API_BASE_URL=https://your-raincloud-domain/api
# VITE_AUTH_BASE_URL=https://your-raincloud-domain

# ============================================================================
# Logging & Debug
# ============================================================================

# Log level: debug, info, warn, error
LOG_LEVEL=info

# ============================================================================
# YouTube Authentication (optional - fixes 403 errors)
# ============================================================================

# Path to Netscape cookies file for yt-dlp authentication
# Export cookies from your browser using a browser extension
# YTDLP_COOKIES=/path/to/cookies.txt
# YTDLP_PATH=yt-dlp
