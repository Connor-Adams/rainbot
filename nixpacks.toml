[phases.setup]
nixPkgs = ["nodejs_22", "python3", "ffmpeg", "yt-dlp"]

[phases.install]
# Skip youtube-dl-exec binary download - we use system yt-dlp from nixpkgs
cmds = ["YOUTUBE_DL_SKIP_DOWNLOAD=true npm install"]

[phases.build]
cmds = ["npm run build || true"]

[start]
cmd = "npm start"

