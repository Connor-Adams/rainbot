# Future work (TODOs)

Short list of tracked TODOs; implement when prioritised.

- **Seek API (UI)** – `ui/src/components/NowPlayingCard.tsx`: progress bar click would seek to position once the Raincloud/worker API supports seek.
- **TTS stream mixing** – `utils/voice/ttsPlayer.ts`: implement true stream mixing so TTS can play alongside music instead of skipping when music is playing.
